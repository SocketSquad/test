// Attachment types

export enum AttachmentType {
  IMAGE = 'image',
  FILE = 'file',
}

export enum AttachmentStatus {
  PENDING = 'pending',
  PROCESSING = 'processing',
  COMPLETED = 'completed',
  FAILED = 'failed',
}
// Friend types

export enum FriendStatus {
  PENDING = 'pending',
  ACCEPTED = 'accepted',
  BLOCKED = 'blocked',
}
